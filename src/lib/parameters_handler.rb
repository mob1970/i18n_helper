class ParametersHandler
  def self.extract(parameters=[])
    options = {}
    parameters.each do |parameter|
      process_parameter(options
    end
  end

  private

  def process_parameter()
  end

end
